int acel_limit = 101;
int avg;

int filter(){
    //sudo code//
    avg = sum(values)/n;
    if(abs(new - agv) > acel_limit){
        //no nothing
    } else {
        //re-order array + add new value
    }
    // Return speed
    return most_recent_valid_speed
}